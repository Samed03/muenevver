// Hier kommen die Tabellen fuer die verwaltbaren Inhalte hin
// (z.B. Reiseziele, Hobbies), sobald der Umfang feststeht.
//
// Beispiel (analog zu vue-playground):
//
// import { int, mysqlTable, varchar } from 'drizzle-orm/mysql-core'
//
// export const travelDestinations = mysqlTable('travel_destinations', {
//   id: int('id').autoincrement().primaryKey(),
//   label: varchar('label', { length: 255 }).notNull()
// })

export {}
